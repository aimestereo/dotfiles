return {
  "aimestereo/dotenv.nvim",
  config = {
    -- event = "VimEnter",
    enable_on_load = true, -- will load your .env file upon loading a buffer
    verbose = true, -- show error notification if .env file is not found and if .env is loaded
  },
}
