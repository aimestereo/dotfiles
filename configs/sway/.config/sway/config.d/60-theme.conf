# Theme bindings
# Opens an interactive theme-bg-set picker in a one-shot ghostty window.
# Absolute path because sway's `exec` spawns a non-login bash that doesn't
# pick up `~/.local/bin` from the user's interactive rc files.
bindsym $mod+Shift+b exec $term -e $HOME/.local/bin/theme-bg-set
