# Multi-Agent Team Workflow

Coordinate specialized agents to implement complex tasks.

## Arguments

- `$ARGUMENTS` - Task description

## Instructions

This command uses the same workflow as `/feat`. Read the `git-workflow` and `orchestrate` skills and follow the orchestration protocol with task: `$ARGUMENTS`.
